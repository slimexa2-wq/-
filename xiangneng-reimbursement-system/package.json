{
  "name": "xiangneng-reimbursement-starter",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "python3 -m http.server 5173",
    "test": "node --test tests/*.test.js",
    "check": "node scripts/check-syntax.mjs",
    "verify": "npm test && npm run check"
  }
}
