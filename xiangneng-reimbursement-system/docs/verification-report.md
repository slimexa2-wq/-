# V0.2 验证报告

## 2026-07-26 本地验证

### 已执行

```bash
npm run verify
```

结果：

- Node Test：13/13 通过；
- JavaScript 语法检查：8/8 文件通过；
- 主要页面模板运行时生成：7/7 通过。

### 已验证业务规则

- 项目可为空；
- 发票金额必须严格大于报销金额；
- 表1、表2、表3由同一明细自动计算；
- 两套打印包分开并沿用表3序号；
- 七个状态按顺序整批流转；
- 禁止跳过审批状态；
- 问题标记不改变主状态；
- 未解决问题时禁止整批通过；
- 发票金额不足时禁止提交。

### 环境限制

当前执行环境的 Chromium 策略阻止访问本地 loopback/file URL，因此未在本环境完成真实浏览器自动点击录制。代码已提供可交互事件绑定与纯业务测试；应在本地 Chrome 或 Codex Browser 中继续执行端到端点击验收。
